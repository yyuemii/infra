apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: connect
  namespace: op-connect
spec:
  releaseName: connect
  interval: 1h
  timeout: 5m
  chart:
    spec:
      chart: connect
      sourceRef:
        kind: HelmRepository
        name: 1password
        namespace: op-connect
      interval: 1h
  values:
    connect:
      credentialsName: op-connect
      credentialsKey: op-connect
    operator:
      create: true
      token:
        name: op-connect
        key: token